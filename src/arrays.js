/**
 * Array of numbers
 * @type {Array}
 */

const numbers = [1, true, [], 2, 3, 4, 5, 6, 7, 8, 9, 0]

/**
 * Array Of User's Scores
 * @type {Array<number>}
 */
const scores = [300, 500, 100]

/**
 * Array Of numbers and strings
 * @type {Array<number|string>}
 */
const scoresAndStrings = [300, 500, 100, "2000"]

/**
 * Array Of numbers and strings
 * @type {Array<string>}
 */
const strings = ["text"]
strings.push('text 2') // Argument of type 'number' is not assignable to parameter of type 'string'.ts(2345)

