/**
 * New user object ("interface")
 * @typedef {object} User
 * @property {number} id User id
 * @property {string} name User's name
 * @property {number|string} [age] User's age (Optional)
 * @property {boolean} isActive User state
 */

/**
 * @type {User}
 */
const newUser = {
  age: 20,
  id: 90,
  isActive: false,
  name: 'Brandon'
}

/**
 * @type {User}
 */
const newUser2 = {
  age: 20,
  id: 90,
  isActive: false,
  name: 'Brandon'
}